config:
  # namespaces: 100
  # projectsPerNs: 10
  # appsPerProject: 10
  # leafAppsPerAppOfApp: 10
  namespaces: 100
  projectsPerNs: 10
  appsPerProject: 1
  leafAppsPerAppOfApp: 2
  repoURL: "https://gitea-git1.apps.managed1.v6vbg.gcp.redhatworkshops.io/admin/gitops-examples.git"
  targetRevision: "main"

  # Base path in the repo under which the charts live.
  basePath: "argocd-agent"

  # Namespace where Argo CD watches for Application resources.
  argocdNamespace: "openshift-gitops"

  # Leave leaves unautomated so they populate the UI without
  # hammering the repo-server. Set to true to exercise full sync.
  automateLeaves: true
